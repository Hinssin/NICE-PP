#include <stdio.h>
#include <math.h>
#define PI 3.14159
int main()
{
	float x, y, z;
	printf("请输入下x,y:");
	scanf("%f%f", &x, &y);
	z = (sin(PI / 6) + 2 * exp(x)) / 2 * y + pow(y, x);
	printf("%f\n", z);
	return 0;
}
